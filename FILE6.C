#include<stdio.h>
#include<conio.h>
int main()
{
	FILE  *fs;
	string c;
	//int len;
	fs=fopen("strwrite.txt","r");
	if(fs==NULL)
	{
		printf("Can't open\n");
		exit(0);
	}
	while(1)
	{
		c=
	}
}
